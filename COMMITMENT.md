# Nabd Agent v2 — الالتزام بالخطة

## المبدأ

لا يخرج Nabd عن نطاق المهمة المطلوبة. ينفّذ الوكيل العمل داخل جذر المشروع فقط، ويعتبر النجاح مثبتًا بالأدلة الفعلية لا باستنتاجات النموذج.

## المكونات الحالية

| المكوّن | الحالة |
|---|---|
| FSM | مكتملة؛ تمنع الانتقال المباشر من `PLANNING` إلى `COMPLETED` وتتعامل مع `REJECTED` كحالة نهائية. |
| EvidenceStore v2 | مكتمل؛ يتحقق من RawFacts بإعادة قراءة القرص، ويربط الأدلة بـ task_id وoperation_id وSHA-256 وfresh/relevant/valid. |
| Workspace Jail | مكتمل؛ يمنع المسارات الخارجية و`.git` ومسارات الأسرار، ويمنع هروب الروابط الرمزية والأوامر الخطرة المعروفة. |
| أدوات المشروع | الأدوات الخمس تعيد RawFacts فقط بعد WorkspaceJail؛ لا تصدر OBSERVED بنفسها. وToolExecutor يحتوي أخطاء JailError وأخطاء الملفات كـ ToolResult فاشل بدل إسقاط الجلسة، ويمنع تنفيذ اسم أداة كأنه أمر shell، مع نسخ احتياطي تلقائي قبل تعديل ملف موجود. |
| `nabd-verify` | يدعم `hash` و`verify` و`jail` و`backup`. |
| Rust verifier | نسخة تاريخية اختيارية داخل `rust-verifier/`؛ **Python هي مصدر الحقيقة الوحيد**. |
| الاختبارات | **60 اختبارًا ناجحًا** في آخر فحص شامل، منها اختبارات RawFacts وtask_id وfresh وrelevant وكشف العبث وبوابة FSM واختيار NVIDIA ومسار الإصلاح بعد فشل سابق واحتواء JailError وتصفية أوامر التحقق الوهمية وعزل آثار `.nabd` من البحث والجرد. |

## قواعد التشغيل

يعمل Nabd افتراضيًا دون طلب موافقة تفاعلية، بناءً على طلب المستخدم. لا يعني ذلك `Secure by default = default=False`؛ بل تعني الحماية هنا أن قيود Jail والأدلة وFSM إلزامية حتى في الوضع التلقائي. يمكن إعادة تفعيل الموافقة يدويًا باستخدام:

```sh
nabd "المهمة" --confirm
```

عدم طلب الموافقة لا يلغي القيود البرمجية الثابتة؛ إذ تستمر `Workspace Jail` في رفض المسارات خارج المشروع والأوامر الخطرة المعروفة، ويستمر شرط الدليل `OBSERVED` قبل إعلان الإكمال.

## قواعد الإثبات

لا يكفي أن يقول النموذج إنه أنشأ ملفًا أو أصلح مشكلة. الأدوات تعيد `RawFacts` فقط، ولا تنتج `OBSERVED`. يقوم `EvidenceStore.verify()` بإعادة قراءة القرص والتحقق من `task_id` و`operation_id` و`relevant` و`fresh` و`valid` والبصمة الحالية. إذا تغيّر الملف أو قُدمت أدلة مهمة قديمة، تفشل عملية التحقق.

لا يعني نجاح أمر shell أو وجود نتائج بحث أن المهمة اكتملت. لا يسمح FSM بالانتقال إلى `COMPLETED` إلا عبر بوابة `complete(verified=True)` بعد نجاح `is_usable_for_completion(task_id)`. لا تُستخدم الأدلة المستنتجة لمحاولة فاشلة لإثبات الإكمال، لكنها لا تسمّم مهمة الإصلاح إلى الأبد؛ إذ يجب أن يثبت الإصلاح اللاحق نفسه بأدلة OBSERVED جديدة وصالحة.

## قواعد النطاق

لا تُضاف واجهة مستخدم أو ميزات بحث أو خدمات خلفية أو تكاملات جديدة ما لم يطلبها المستخدم صراحة. يجب أن ينجح الأمر التالي قبل تسليم أي نسخة:

```sh
python3 -m unittest discover -s tests -v
```

## التحقق الأخير

آخر نسخة مسلّمة هي نسخة Termux التي تعمل افتراضيًا دون موافقة، وتحتوي على RawFacts وEvidenceStore v2 والأدوات الخمس ودعم OpenAI وGemini وNVIDIA NIM والتوثيق.

**مبدأ أساسي:** Python هي مصدر الحقيقة الوحيد. أي تغيير لاحق يجب أن يبدأ من `nabd/*.py` ويُثبت بـ `python3 -m unittest discover -s tests -v`. النسخة Rust اختيارية تاريخية وليست جزءًا من بنية الاختبار.

لا تُضاف واجهة مستخدم أو ميزات بحث أو خدمات خلفية جديدة إلا بطلب صريح.
