# Taste Profile
- Communicates in Arabic (e.g., "سحب اخر تحديثات المستودع"); expects replies, explanations, and summaries in Arabic while keeping code, commands, and identifiers in English. Confidence: 0.9
- Prefers NVIDIA NIM as the LLM provider for the Nabd-Agent project (supplied an `nvapi-` API key and had `NABD_PROVIDER=nvidia` set in `.env.local`). Confidence: 0.6
- Handles API credentials via a git-ignored local env file (`cp .env.example .env.local`, then source it with `. ./.env.local` before running); when verifying setup, checks that keys are loaded (presence/length) without printing the secret value. Confidence: 0.7
- Bug-fix workflow: first inspect repository state (README, config, related files) without assuming project structure or error cause; diagnose and prove the root cause from code/tests/logs (cite file:line evidence); reproduce the problem with a small test or command before editing code; apply the minimal targeted fix at the single containment layer that preserves current design and cross-project compatibility; add a regression test reproducing the exact failure beside related tests; then review the final change for side effects, edge cases, and compatibility regressions (including any secret/unsafe-path exposure). Confidence: 0.85
- Staged verification discipline: run the targeted/related test first, then the full suite, then lint/type-check/build if the project uses them; any failure (and warnings, not just errors) counts as non-success and must be fixed by reading the full error and iterating to a fully green result before declaring done. Confidence: 0.9
- Refuses destructive Git operations and history rewrites: never uses `git reset --hard` or `git clean -fd`, never changes Git history, never deletes or replaces pre-existing local changes, and never commits or pushes unless explicitly asked. Confidence: 0.9
- Never modifies environment/secret files or embeds API keys/tokens anywhere in code, tests, or commits. Confidence: 0.9
- Tests with Python stdlib unittest, not pytest: `python3 -m unittest discover -s tests -v` (the Nabd-Agent test suite is unittest-based). Confidence: 0.8
- Expects bug fixes to end with a structured Arabic report: goals checklist, proven root cause with evidence, a changed-files table (file / change / reason), tests added/changed, all verification commands and their results, pass/fail counts, remaining risks/constraints, and a commit-message-style summary provided without actually committing. Confidence: 0.8
- Insists on agent self-isolation / Ground Truth integrity: the agent's own runtime artifacts (`.nabd/` evidence store + backups) must be excluded from every tool's output so they never enter model context — otherwise the model could modify its own evidence and corrupt the ground truth (extends the credential-secrecy concern to agent-state secrecy). Confidence: 0.9
- Robust multi-format hallucination filtering: when stripping model-emitted tool names from verification lists, must handle `<tool>: <cmd>` (colon separator), `<tool> <cmd>` (space separator), and bare tool names — never assumes a single format, since the model varies it unpredictably across runs. Confidence: 0.9
- Canonicalizes non-deterministic tool output (e.g., sorts ripgrep `matches`) so behavior and tests are deterministic rather than flaky; will run a target test repeatedly (e.g., 10x) to prove the flake is gone. Confidence: 0.8
- Applies data-leakage / integrity fixes defense-in-depth across every tool that could expose artifacts — extended `.nabd/` exclusion from `SearchTool` to `ListTool` (initial inventory) — rather than patching only the first point of discovery. Confidence: 0.85
- Prefers a single clean pre-launch commit: amends the not-yet-pushed local commit rather than stacking multiple commits, and checks commit-message metadata (e.g., `Co-authored-by` trailers) is spelled correctly before finalizing. Confidence: 0.85
- Verifies fixes end-to-end in the real environment with adversarial fixtures pre-seeded in the workspace (e.g., stale `.nabd/evidence.json` + `.nabd/backups/*`), proving the agent never leaks them and completes the task to `COMPLETED` — beyond unit tests alone. Confidence: 0.85
- Uses a not-yet-pushed local commit + an ephemeral `export`-injected API key for demos: the secret is typed inline for the run but is guaranteed never to be written to files or committed to the repo (rotated/removed afterward). Confidence: 0.75
- Runs execution under a **SUPERVISED_ONLY** gate: requires explicit authorization between phases and never autonomously advances to a "memory" or next phase without sign-off; frames handoffs as "AWAITING_AUTHORIZATION". Confidence: 0.9
- On failure in a supervised protocol, **aborts rather than silently fixing**: if exit code is non-zero, any test fails, or raw CLI output can't be retrieved, triggers an immediate abort and awaits instructions instead of stubbing, working around missing artifacts, or auto-remediating repository-state mismatches. Confidence: 0.85
- **CLI_MEASURED evidence over inference**: prohibits inferring success without running the specified command; demands the entire raw stdout/stderr pasted verbatim plus the exact exit code, and will not declare pass/fail from assumption. Confidence: 0.85
- Executes commands **verbatim as specified**: treats the user-provided command as the exact terminal instruction (only appending `2>&1` for capture) and expects it run precisely, not re-derived or substituted. Confidence: 0.85
- Demands **zero conversational filler** with an exact structured report format (fixed section headers like [OBSERVED], [INFERRED], etc.): rejects explanatory prose beyond the required schema and expects strict adherence to the prescribed output template. Confidence: 0.9
